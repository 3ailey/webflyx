info:

- arguments in angled brackets (<>) are mandatory and must be included when running a command
- arguments in squared brackets ([]) are optional and can be included if necessary
- titles blob hash 8577369
